#include <stdio.h>

/**
 * main - prints alll string from 0 - 9
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("\n");
	}

	printf("\n");
	return (0);
}
