#include "main.h"

/**
 * _memset - this memory set function
 * @s: string
 * @c: character
 * @n: integer
 * Return: a string
 */
char *_memset(char *s, char b, unsigned int n);
{
	int i = 0;

	for (; n > 0; i++)
	{
		s[a] = b;
		n--;
	}
	return (s);
}
